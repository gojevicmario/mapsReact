import React, {useEffect, useState} from 'react'

export default function MapifyTable({columns, data}) {
  var test = columns;
  return (
    <table className="table table-striped">
      <thead>
        <tr>
          
        </tr>
      </thead>
    </table>
  )
}
